#!/usr/bin/env bash
# VSP Phone — refresh local Telnyx knowledge base
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${REPO_ROOT}"
node scripts/update-telnyx-docs.js "$@"
