# Conferences

Conference-related Call Control API docs are indexed under `call-control/api-reference/` (e.g. conference commands).

Run `node scripts/update-telnyx-docs.js` to refresh. See `../search-index.json` for the full list.
