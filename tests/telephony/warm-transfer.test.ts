import { describe, it } from 'vitest';

/**
 * Warm / attended transfer — planned v1.3.
 */
describe('telephony / warm transfer (planned)', () => {
  it.todo('POST /api/softphone/transfer/attended/start — v1.3');
  it.todo('POST /api/softphone/transfer/attended/complete — v1.3');
  it.todo('POST /api/softphone/transfer/attended/cancel — v1.3');
});
