import { describe, it } from 'vitest';

/**
 * Call queues (ACD) — not implemented. Telnyx enqueue planned v1.5.
 */
describe('telephony / queue (planned)', () => {
  it.todo('POST /api/tenant/queues — v1.5');
  it.todo('Telnyx enqueue on no-agent — v1.5');
  it.todo('queue wallboard metrics — v1.5');
});
